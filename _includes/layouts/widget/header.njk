<header>
<h1><a href="{{ '/' | url }}" class="link">{{ metadata.title }}</a></h1>
<h2 class="desc">{{ metadata.description }}</h2>
{%- for entry in collections.all | eleventyNavigation %}
<a href="{{ entry.url | url }}" class="link">{{ entry.title }}</a>
{%- endfor %}
</header>